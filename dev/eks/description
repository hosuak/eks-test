# [ provider ] terraform이 리소스를 생성하고 관리하는 데 사용하는 plugin 
#   - provider는 테라폼 코드가 어떤 대상에 적용할지 설정
#   - provider가 제공하는 api를 호출해 상호작용
#   - terraform block에 설정
#   - required_provider인자에 provider이름과 버전을 설정

; [vpc]
;  - kubernetes.io/cluster/cluster명 = shared  : eks 버전 1.18이하 
;  - kubernetes.io/cluster/cluster명 = owned  : eks 버전 1.14이하 버전 사용시 tag에 추가했었다.
;  - kubernetes.io/cluster/elb = 1  : 퍼블릭 lb
;  - kubernetes.io/cluster/internal-elb = 1  : 프라이빗 lb 사용 시 추가 해야함.

; [subnet]
;  - tag 중 subnetTyep = utility가 있는데 utility 서브넷은 일반적으로 관리 도구, 로깅, 모니터링 시스템, 
;    그리고 클러스터 내외부 간의 네트워크 연결과 같은 클러스터 관리 관련 작업을 처리하는 서브넷이다.

